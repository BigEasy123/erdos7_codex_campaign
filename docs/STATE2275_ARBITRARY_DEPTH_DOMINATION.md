# State-2275 Arbitrary-Depth Domination

Status: **BROKEN / OPEN**

The required statement is that every genuine arbitrary-depth continuation maps to a feasible point of the exact local relaxation. This has not been proved.

The current pooled builder remains float-dependent in unresolved deep-3, tail, and conditional child-capacity coefficients. The exact source reconstruction establishes exact upstream families for several tower and Hunter quantities, but it does not establish the direction of every pooled inequality.

The unresolved obligations are:

- further powers of 3, 5, 7, and 11;
- mixed repeated powers and unresolved tails;
- shared next-3 child choices;
- divisor-completion rows;
- Hunter/CRT overlap rows;
- future support variables and their relaxation direction.

Therefore the implication

```text
 genuine arbitrary-depth continuation => feasible exact pooled-model point
```

remains unproved. The numerical bound near `8.764` cannot be promoted to `sum(e) > 8`.

No state-2275 arbitrary-depth theorem is claimed.
